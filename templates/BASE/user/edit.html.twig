{% extends 'base.html.twig' %}

{% block title %} {{ title_general }} {% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    {{ parent() }}
    <div class="container">
        <h1>Edit User</h1>

        {{ include('BASE/user/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_user_index') }}">Back to list</a>

        {{ include('BASE/user/_delete_form.html.twig') }}
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
